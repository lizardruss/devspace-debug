version: v2beta1
localRegistry:
  enabled: false
images:
  back-service:
    image: lizardruss/back-service
    skipPush: true

deployments:
  back-service:
    helm:
      values:
        labels:
          service: back-service
        containers:
          - image: lizardruss/back-service

dev:
  back-service:
    imageSelector: lizardruss/back-service
    devImage: node:18.8-alpine
    workingDir: /usr/app
    command: ["npm", "run", "dev"]
    logs: {}
    persistPaths:
      - path: /usr/app
      - path: /.devspace
    persistenceOptions:
      size: 2Gi
      accessModes:
        - ReadWriteMany
      name: back-service-pvc
    sync:
      - path: ./
        initialSync: preferLocal
        startContainer: true
        printLogs: true
        disableDownload: true
        onUpload:
          exec:
            - command: "npm install"
              onChange: ["./package-lock.json"]
