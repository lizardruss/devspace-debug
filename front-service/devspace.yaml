version: v2beta1
localRegistry:
  enabled: false
images:
  front-service:
    image: lizardruss/front-service
    skipPush: true

deployments:
  front-service:
    helm:
      values:
        labels:
          service: front-service
        containers:
          - image: lizardruss/front-service

dev:
  front-service:
    imageSelector: lizardruss/front-service
    devImage: node:18.8-alpine
    workingDir: /usr/app/front-service
    command: ["npm", "run", "dev"]
    logs: {}
    persistPaths:
      - path: /usr/app
        volumePath: front-service
    persistenceOptions:
      size: 5Gi
      accessModes:
        - ReadWriteMany
      name: pnpm-pvc
    sync:
      - path: ./
        initialSync: preferLocal
        printLogs: true
        disableDownload: true
        onUpload:
          restartContainer: true
          exec:
            - command: "npm install"
              onChange: ["./package-lock.json"]
