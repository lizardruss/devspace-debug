version: v2beta1
localRegistry:
  enabled: false
images:
  front-service:
    image: lizardruss/front-service
    skipPush: true

deployments:
  front-service:
    helm:
      values:
        labels:
          service: front-service
        containers:
          - image: lizardruss/front-service

dev:
  front-service:
    imageSelector: lizardruss/front-service
    devImage: node:18.8-alpine
    workingDir: /usr/app
    command: ["npm", "run", "dev"]
    logs: {}
    persistPaths:
      - path: /usr/app
      - path: /.devspace
    persistenceOptions:
      size: 2Gi
      accessModes:
        - ReadWriteMany
      name: front-service-pvc
    sync:
      - path: ./
        initialSync: preferLocal
        startContainer: true
        printLogs: true
        disableDownload: true
        onUpload:
          exec:
            - command: "npm install"
              onChange: ["./package-lock.json"]
